[phases.setup]
nixPkgs = ['php82', 'php82Extensions.mysqli', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql', 'php82Extensions.mbstring']

[phases.install]
cmds = ['echo "No install needed"']

[start]
cmd = 'php -S 0.0.0.0:$PORT -t .'